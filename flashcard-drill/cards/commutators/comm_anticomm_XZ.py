"""Card: comm_anticomm_XZ"""
from __future__ import annotations
from core.models import Flashcard

CARD = Flashcard(
    id='comm_anticomm_XZ',
    category='Commutators',
    front='{X, Z} = ?',
    back='{X, Z} = XZ + ZX = 0   — all distinct Pauli matrices anticommute',
)
