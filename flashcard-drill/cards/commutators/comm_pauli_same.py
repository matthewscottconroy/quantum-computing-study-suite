"""Card: comm_pauli_same"""
from __future__ import annotations
from core.models import Flashcard

CARD = Flashcard(
    id='comm_pauli_same',
    category='Commutators',
    front='[X, X] = ?',
    back='[A, A] = 0 for any operator',
)
