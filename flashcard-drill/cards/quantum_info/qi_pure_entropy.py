"""Card: qi_pure_entropy"""
from __future__ import annotations
from core.models import Flashcard

CARD = Flashcard(
    id='qi_pure_entropy',
    category='Quantum Info',
    front='S(|ψ⟩) for a pure state?',
    back='S = 0  — pure states have zero entropy',
)
