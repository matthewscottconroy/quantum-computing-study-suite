"""Card: gate_SZSdag"""
from __future__ import annotations
from core.models import Flashcard

CARD = Flashcard(
    id='gate_SZSdag',
    category='Gate Unitaries',
    front='SZS† = ?',
    back='SZS† = Z   — S and Z are both diagonal, so S commutes with Z',
)
