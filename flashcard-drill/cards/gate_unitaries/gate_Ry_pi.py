"""Card: gate_Ry_pi"""
from __future__ import annotations
from core.models import Flashcard

CARD = Flashcard(
    id='gate_Ry_pi',
    category='Gate Unitaries',
    front='Ry(π) = ?',
    back='Ry(π) = −iY   — rotation by π around Y equals Y up to global phase −i',
)
