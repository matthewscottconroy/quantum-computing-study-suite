"""Card: gate_H2"""
from __future__ import annotations
from core.models import Flashcard

CARD = Flashcard(
    id='gate_H2',
    category='Gate Unitaries',
    front='H² = ?',
    back='H² = I   — Hadamard is its own inverse',
)
