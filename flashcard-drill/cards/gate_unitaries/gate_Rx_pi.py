"""Card: gate_Rx_pi"""
from __future__ import annotations
from core.models import Flashcard

CARD = Flashcard(
    id='gate_Rx_pi',
    category='Gate Unitaries',
    front='Rx(π) = ?',
    back='Rx(π) = −iX   — rotation by π around X equals X up to global phase −i',
)
