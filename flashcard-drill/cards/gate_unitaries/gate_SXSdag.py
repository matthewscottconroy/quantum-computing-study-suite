"""Card: gate_SXSdag"""
from __future__ import annotations
from core.models import Flashcard

CARD = Flashcard(
    id='gate_SXSdag',
    category='Gate Unitaries',
    front='SXS† = ?',
    back='SXS† = Y   — conjugation by S maps X→Y (and Y→−X)',
)
