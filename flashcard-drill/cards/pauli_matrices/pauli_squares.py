"""Card: pauli_squares"""
from __future__ import annotations
from core.models import Flashcard

CARD = Flashcard(
    id='pauli_squares',
    category='Pauli Matrices',
    front='X² = Y² = Z² = ?',
    back='X² = Y² = Z² = I',
)
