"""Card: cx_IP_PSPACE"""
from __future__ import annotations
from core.models import Flashcard

CARD = Flashcard(
    id='cx_IP_PSPACE',
    category='Complexity',
    front='IP = ?',
    back='IP = PSPACE  (Shamir 1992) — interactive proofs with unbounded prover capture exactly polynomial space',
)
