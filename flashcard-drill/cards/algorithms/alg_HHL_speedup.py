"""Card: alg_HHL_speedup"""
from __future__ import annotations
from core.models import Flashcard

CARD = Flashcard(
    id='alg_HHL_speedup',
    category='Algorithms',
    front='HHL algorithm: claimed speedup for Ax=b?',
    back='Exponential speedup in problem size n, but output is a quantum state — classical readout may negate speedup',
)
